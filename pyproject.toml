[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "soccer-analytics"
version = "0.1.0"
description = "Soccer analytics research package."
requires-python = ">=3.12"
